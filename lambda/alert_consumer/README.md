# Lambda alert_consumer

## Propósito

**Trigger SQS:** lee mensajes de alerta y escribe logs de nivel CRITICAL en CloudWatch (`URGENCIA IoT | ...`).

## Build

Incluido en `make lambda-build` / `lambda/build_all.sh`.
