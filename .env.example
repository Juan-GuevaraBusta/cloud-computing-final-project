# Copiar a .env y completar con valores reales. Nunca subir .env a Git.
# En AWS, Lambda y ECS reciben MONGODB_URI desde Terraform (no desde este archivo).

MONGODB_URI=mongodb://usuario:CAMBIAR_PASSWORD@EC2_PRIVATE_IP:27017/iot?authSource=admin
AWS_REGION=us-east-1
